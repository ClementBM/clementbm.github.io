name: CheckLinks
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  checklinks:
    name: Linux
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Ruby
        uses: actions/setup-ruby@v1
        with:
          ruby-version: 2.6.x
      - name: Setup Rubygems, install dependencies
        run: | 
          gem update --system --no-document
          gem update bundler --no-document
          bundle install
      - name: Build jekyll website with drafts
        run: bundle exec jekyll build --drafts
      - name: Check for broken links
        id: "broken-link-check"
        run: |
          echo "::set-output name=link_check::`$(bundle exec htmlproofer --log-level :debug ./_site 2>&1)`"
        continue-on-error: true
      - name: Save and show
        run: |
          echo "${{ steps.broken-link-check.outputs.link_check }}"
          echo "${{ steps.broken-link-check.outputs.link_check }}" > links.log
      - name: Archive log links
        uses: actions/upload-artifact@v1
        with:
          name: link-check.log
          path: links.log